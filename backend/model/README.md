# This directory holds the UniXCoder ONNX model.
# The model is downloaded automatically during Docker build.
# To download manually:
#   pip install huggingface-hub
#   python -c "from huggingface_hub import hf_hub_download; hf_hub_download('microsoft/unixcoder-base', 'onnx/model.onnx', local_dir='.')"
